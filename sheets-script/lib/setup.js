function setup() {
  // check that setup has not already been run
  // set query parameters as keys in cells
  // automatically create button to run fetchNews function
  // create table for results
  // lock cells
  // store metadata indicating setup has been done
  return;
}
